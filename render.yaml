services:
  - type: web
    name: dublin-mobility-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api:app --host 0.0.0.0 --port 10000
    pythonVersion: "3.11.9"
    envVars:
      - key: DATABASE_URL
        sync: false
